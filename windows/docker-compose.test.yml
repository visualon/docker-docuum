services:
  sut:
    image: "${IMAGE}"
    entrypoint: ''
    command: docuum --version && docker info
    volumes:
      - source: '\\.\pipe\docker_engine'
        target: '\\.\pipe\docker_engine'
        type: npipe
